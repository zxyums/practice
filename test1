<TextField
  {...field}
  multiline
  fullWidth
  rows={4} // Minimum visible rows
  maxRows={10} // Maximum visible rows before internal scrolling
  placeholder="Enter your comments"
  InputProps={{
    style: {
      height: "auto", // Allow dynamic height adjustment
      maxHeight: "200px", // Constrain maximum height of the text area
      overflowY: "auto", // Add vertical scrolling for overflow
      overflowX: "hidden", // Prevent horizontal scrolling
      wordBreak: "break-word", // Break long words into the next line
      whiteSpace: "pre-wrap", // Preserve spaces and line breaks
    },
  }}
  sx={{
    "& .MuiOutlinedInput-root": {
      alignItems: "flex-start", // Align text to the top
      height: "auto", // Allow the container to grow dynamically
      "& textarea": {
        height: "auto", // Allow the textarea to resize dynamically
        maxHeight: "200px", // Constrain height to avoid excessive growth
        overflowY: "auto", // Enable vertical scrolling
        overflowX: "hidden", // Prevent horizontal scrolling
        resize: "none", // Disable manual resizing
        wordBreak: "break-word", // Ensure long words wrap correctly
        whiteSpace: "pre-wrap", // Preserve line breaks and spaces
      },
      "& fieldset": {
        borderColor: "gray", // Default border color
        borderWidth: "1px", // Ensure uniform border thickness
      },
      "&:hover fieldset": {
        borderColor: "black", // Hover border color
        borderWidth: "1px", // Ensure hover styling for top border
      },
      "&.Mui-focused fieldset": {
        borderColor: "blue", // Focus border color
        borderWidth: "2px", // Slightly thicker focus border
      },
    },
  }}
  style={{
    maxHeight: "250px", // Restrict overall height to enable browser scroll
    overflowY: "auto", // Add browser scroll when text field exceeds screen
  }}
/>